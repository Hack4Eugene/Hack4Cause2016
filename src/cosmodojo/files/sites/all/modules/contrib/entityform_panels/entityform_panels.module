<?php
/**
 * @file
 * Entityforms panels integration
 */


/**
 * Implements hook_ctools_plugin_directory().
 */
function entityform_panels_ctools_plugin_directory($module, $plugin) {
  if (is_dir(dirname(__FILE__) . '/plugins/' . $plugin)) {
    return 'plugins/' . $plugin;
  }
}
